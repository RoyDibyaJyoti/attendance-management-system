# AMCS Local PostgreSQL Environment Variables
# Copy this file to .env and adjust values as needed.

POSTGRES_DB=amcs_db
POSTGRES_USER=amcs_user
POSTGRES_PASSWORD=amcs_secret_password
POSTGRES_PORT=5432
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/amcs_db
